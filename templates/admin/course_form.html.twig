{% extends 'admin/base.html.twig' %}

{% block title %}{{ title }} – FTIC Admin{% endblock %}

{% block body %}
<div class="mb-6 flex items-center justify-between">
    <div>
        <h1 class="text-2xl font-bold text-gray-900">{{ title }}</h1>
        <p class="text-sm text-gray-500 mt-1">Fill in the details below</p>
    </div>
    <a href="{{ path('app_admin_courses') }}"
       class="inline-flex items-center gap-2 text-sm font-medium text-gray-600 px-4 py-2 rounded-lg border border-gray-200 bg-white hover:bg-gray-50 transition">
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"/>
        </svg>
        Back to Courses
    </a>
</div>

{{ form_start(form, {attr: {class: 'space-y-6', novalidate: 'novalidate'}}) }}

<div class="bg-white rounded-xl shadow-sm border border-gray-100 p-6 space-y-6">

    {# Course Name #}
    <div>
        {{ form_label(form.name, 'Course Name', {label_attr: {class: 'block text-sm font-medium text-gray-700 mb-1'}}) }}
        {{ form_widget(form.name, {attr: {
            class: 'w-full px-4 py-2.5 text-sm border border-gray-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-offset-0 bg-gray-50',
            style: 'focus:ring-color:#0b3612;',
            placeholder: 'e.g. Basic Fire Safety Training'
        }}) }}
        {{ form_errors(form.name) }}
    </div>

    {# Description #}
    <div>
        {{ form_label(form.description, 'Description', {label_attr: {class: 'block text-sm font-medium text-gray-700 mb-1'}}) }}
        {{ form_widget(form.description, {attr: {
            class: 'w-full px-4 py-2.5 text-sm border border-gray-200 rounded-lg focus:outline-none focus:ring-2 bg-gray-50 resize-none',
            rows: 4,
            placeholder: 'Briefly describe what this course covers…'
        }}) }}
        {{ form_errors(form.description) }}
    </div>

    {# Trainer #}
    <div>
        {{ form_label(form.trainer, 'Assign Trainer', {label_attr: {class: 'block text-sm font-medium text-gray-700 mb-1'}}) }}
        {{ form_widget(form.trainer, {attr: {
            class: 'w-full px-4 py-2.5 text-sm border border-gray-200 rounded-lg focus:outline-none focus:ring-2 bg-gray-50'
        }}) }}
        <p class="mt-1 text-xs text-gray-400">Only users with the Trainer role are shown.</p>
        {{ form_errors(form.trainer) }}
    </div>

    {# Status #}
    <div>
        {{ form_label(form.status, 'Status', {label_attr: {class: 'block text-sm font-medium text-gray-700 mb-1'}}) }}
        {{ form_widget(form.status, {attr: {
            class: 'w-full px-4 py-2.5 text-sm border border-gray-200 rounded-lg focus:outline-none focus:ring-2 bg-gray-50'
        }}) }}
        {{ form_errors(form.status) }}
    </div>

</div>

<div class="flex items-center gap-4">
    <button type="submit"
            class="inline-flex items-center gap-2 text-sm font-semibold text-white px-6 py-2.5 rounded-lg shadow-sm transition"
            style="background-color:#0b3612;" onmouseover="this.style.backgroundColor='#5c8d67'" onmouseout="this.style.backgroundColor='#0b3612'">
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
        </svg>
        Save Course
    </button>
    <a href="{{ path('app_admin_courses') }}" class="text-sm text-gray-500 hover:text-gray-700">Cancel</a>
</div>

{{ form_end(form) }}
{% endblock %}
